# dependencies
node_modules/
.pnpm-store/

# build outputs
dist/
.next/
out/
build/
*.tsbuildinfo

# turbo
.turbo/

# env (commit only .env.example)
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# editor / os
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# test / coverage
coverage/

# embedded-postgres ephemeral test cluster (safety net; also in packages/db/.gitignore)
.pgtmp/
.pgtmp.json
**/.pgtmp/
**/.pgtmp.json

# docker volumes (if ever bound locally)
.pgdata/

# agent runtime locks
.claude/scheduled_tasks.lock
