// app.<root> -> /platform. Super-admin is a later phase (blueprint §11). Clean
// stub on the shared tokens; utilitarian/dense dialect (DESIGN §2), Latin.
export default function PlatformHome() {
  return (
    <main className="min-h-screen bg-bg px-4 py-10" lang="en">
      <div className="mx-auto max-w-admin">
        <h1 className="text-2xl font-bold text-ink">Platform</h1>
        <p className="mt-2 text-sm text-ink-muted">
          Super-admin tooling lands in a later phase.
        </p>
      </div>
    </main>
  );
}
