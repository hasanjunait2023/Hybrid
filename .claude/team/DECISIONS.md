# Decision Log

> Append-only. One dated entry per phase transition or notable call.
> This is the company's memory — it lets `/ceo` resume and the loop survive across sessions.

## 2026-06-23 — CHARTER — M1
- Decided: Chartered mission "Hybrid" (Shopify for Bangladesh). Brand renamed Ecomex → Hybrid. Persisted source docs to `docs/` (PRD, BUILD_CHECKLIST, 01_schema.sql, 02_policies.sql). Created team scaffold. Active milestone M1 = Phase 0 spine.
- Why: Founder invoked /ceo with PRD + schema + policies + checklist. Goal is clear; phased roadmap already defined — adopt it as milestone backlog M1–M5.
- Dispatched: (none yet) — proceeding to gate questions then RESEARCH/PLAN.
- Outcome: Scaffold complete. Asking founder 4 sharp questions that shape the Phase-0/Phase-1 plan before GATE 1.

## 2026-06-23 — RESEARCH — M1
- Decided: Founder gate answers — full-vision scope (loop M1→M5), local-first infra, Steadfast courier, root domain myhybrid.com placeholder. Dispatching focused RESEARCH to de-risk Phase 0 critical path before PLAN.
- Why: Phase-0 spine is well-trodden (Turborepo + Next.js multi-tenant + Supabase RLS) but the withTenant() session-variable RLS layer + connection pooling (pgBouncer transaction mode vs set_config(...,true)) has sharp edges that, if wrong, silently break tenant isolation. De-risk now.
- Dispatched: researcher — Phase-0 critical patterns (withTenant + pooling + RLS session var, Next.js host-based middleware, local-first Postgres for RLS roles).
- Outcome: pending researcher return.

## 2026-06-23 — PLAN — M1
- Decided: RESEARCH complete (docs/research/phase0-brief.md). Key outcomes: (1) BLOCKING DEFECT in 02_policies.sql — app_runtime is NOLOGIN, cannot be connected as; fix via app_runtime_login LOGIN role inheriting app_runtime grants. (2) withTenant() = postgres.js sql.begin + set_config(...,true) inside, prepare:false. (3) local-first: lvh.me + docker postgres:16-alpine. (4) RLS proven via app-level Vitest integration tests in CI.
- Why: De-risked the make-or-break tenant-isolation layer before committing to a design.
- Dispatched: ceo-architect (Phase 0 blueprint, must resolve open risks: app.current_user_id GUC, signup provisioning via asPlatformAdmin, order_counter trigger under RLS) + designer (DESIGN.md — Hybrid brand, Bengali-first + mobile-first system).
- Outcome: pending. Then synthesize → GATE 1.

## 2026-06-23 — GATE1 — M1
- Decided: PLAN complete. Architect delivered buildable Phase-0 blueprint (Turborepo; @hybrid/db withTenant; 00_roles.sql + 04_grant_login.sql bookends fixing NOLOGIN defect, canonical 01/02/03 untouched; kysely-codegen types; local auth stub w/ permanent getSession() seam; middleware host→tenant→/_sites/[tenant]; unstable_cache per-tenant tags; 5-test Vitest RLS CI gate; local-first docker postgres+redis+lvh.me). All brief open-risks resolved (withTenant takes userId; signup via asPlatformAdmin; order_counter under RLS proven; prepare:false). Designer delivered DESIGN.md "Bazaar Modern"/theme "Doreja" — trust-first, Bangla-first (Hind Siliguri), light-mode, COD-green, Bangla numerals on storefront, sticky action bar; dark mode scoped out of P0/1.
- Why: De-risked + locked the make-or-break isolation layer and the design bar before any code.
- Dispatched: ceo-architect + designer (parallel).
- Outcome: Parked at GATE 1 — presenting plan to founder for approval before BUILD.

## 2026-06-23 — BUILD — M1
- Decided: Backend foundation (Slice 0+1) built & statically green (typecheck/lint/build clean; no-raw-sql rule proven; migrate file-selection + seed verified). BLOCKER: no Docker on this machine → live DB / 5-test RLS gate + app_runtime_login connection proof not run locally. Installing local PostgreSQL 16 via winget to execute the gate (Docker-equivalent dev DB). scripts/verify-phase0.sh + CI also run it.
- Accepted minor deviations: Tx type uses postgres.js TransactionSql (blueprint's Parameters<...> form non-callable under strict); dropped eslint-config-next (flaky dep); RLS test beforeAll clears orders/order_counter for determinism (order_counter still not pre-seeded).
- SECURITY (HARDEN blocker, from automated commit review): apps/web/lib/auth/session.ts parseDevCookie validates UUID shape but does NOT verify the HMAC signature and does not refuse production → forgeable dev session cookie (HIGH, auth bypass pattern). Mitigated now (dev-only, route NODE_ENV-guarded, local-first/no prod) but MUST fix before GATE 2 / before Phase-1 real auth: verify HMAC in parseDevCookie + return null in production. Tracked.
- Dispatched: backend-engineer (returned). Next: prove RLS gate on local Postgres, then frontend-engineer (Slice 2+3).
- Outcome: pending local Postgres install → RLS proof.

## 2026-06-23 — HARDEN — M1
- Decided: Static gauntlet (code-reviewer + security-officer) run. Security verdict SAFE-TO-SHIP for local-first P0 with 1 mandatory pre-GATE2 fix; isolation core VERIFIED sound (fail-closed NULL GUC, FORCE RLS, no BYPASSRLS, adminSql not reachable from app, set_config transaction-local, order_counter SECURITY INVOKER passes WITH CHECK, no IDOR — Server Action re-derives tenant server-side). Code-review verdict FIX-FIRST: 0 blockers, 4 majors.
- Fix list before GATE2: (1) HIGH parseDevCookie verify HMAC constant-time + refuse prod [backend session.ts]; (2) middleware lets /dev-login pass through on admin host [backend middleware]; (3) guard /_sites on root host (matcher/notFound) [backend middleware]; (4) resolveTenantByHost filter t.status='active' + redis try/catch fallback [backend resolve.ts]; (5) ?as=admin → route to /platform stub, keep owner-a/b for tenant admin (no redirect loop) [frontend admin layout]; (6 MINOR) tag context cache tenant:{id} + admin variant is_active note [frontend data].
- Dispatched: backend-engineer (continuation) middleware+resolve+session; frontend-engineer (continuation) admin layout/data. Non-overlapping files. QA + live RLS gate after fixes (PostgreSQL 16 installing via winget).
- Outcome: pending fixes + live verification.

## 2026-06-23 — HARDEN (fixes + gate) — M1
- Decided: No Docker + no system PG on this machine; winget PostgreSQL install stalled. PIVOTED the RLS gate to embedded-postgres (npm) in packages/db test harness — runs anywhere, no Docker. RESULT: 5/5 RLS tests PASS live. Proven: (a) app_runtime_login logs in as non-superuser (NOLOGIN defect fix works), (b) cross-tenant INSERT rejected by RLS WITH CHECK, (c) per-tenant order_number independent (A=1,B=1,A=2). = Phase-0 DoD (c) GREEN.
- HARDEN fixes all applied + verified (typecheck/lint/build green): backend — parseDevCookie now HMAC constant-time verify + getSession prod-guard + DEV_SESSION_SECRET fail-fast (security HIGH CLOSED); middleware dev-login pass-through on admin host (MAJOR1); /_sites root-host guard (MAJOR3); resolve status='active' + redis try/catch fallback (MAJOR4/5). frontend — ?as=admin→/platform no loop (MAJOR2/5); context cache tagged tenant:{id}+:theme (MINOR); admin getAdminProducts is_active filter aligned (MINOR).
- NOTE: backend subagent ran `git init` and committed its files (1416ab2) without explicit founder authorization. Local only, not pushed. Flag at RETRO; founder may amend commit policy.
- Dispatched: backend-engineer (fixes), frontend-engineer (fixes), backend-engineer (embedded-pg harness) — all returned green.
- Outcome: All gauntlet blockers cleared. Final step: QA live render+edit smoke against embedded PG, then GATE 2.

## 2026-06-23 — AUTONOMY GRANTED — M1→M5
- Decided: Founder granted full autonomous operation. CEO now self-approves GATE 1 and GATE 2 (gauntlet must still pass first), runs SHIP/RETRO/LOOP, and advances M1→M5 without human gate-blocking. Deferred decisions resolved by sensible default (record each). Notifications pushed at gates; founder may override anytime.
- Why: Founder instruction "make everything autonomously on ceo and loop system, approve what it need to approve."
- Standing rules for self-approval: GATE 2 self-approve ONLY if QA=PASS, code-review=SHIP (or majors fixed), security=SAFE-TO-SHIP (blockers fixed). If a gauntlet blocker can't be cleared, park + notify (do not approve over a real blocker).
- Loop vehicle: local-first → in-session /loop + ScheduleWakeup re-invokes this session (cloud cron rejected — cloud agents lack local repo/DB).

## 2026-06-23 — GATE2 (self-approved) — M1
- Decided: GATE 2 SELF-APPROVED under autonomous authority. Conditions met: QA=PASS (5/5 RLS deterministic; per-tenant render store-a indigo/store-b crimson zero cross-leak; /_sites + unknown-host guarded; admin edit→storefront proven via scripted withTenant + live HTTP re-read + revalidateTag code-match; no-raw-sql enforced live; build/typecheck/lint green); security=SAFE-TO-SHIP (HIGH parseDevCookie fixed — forged cookie rejected); code-review majors all fixed.
- Tech debt logged (non-blocking): (1) Windows-only vitest globalSetup EBUSY rmdir teardown flake → flips exit code + orphan PG on local reruns; Linux CI unaffected. Fix: force-kill cluster + retry rmdir + pre-run .pgtmp sweep. (2) ioredis "Unhandled error event" log spam under sustained Redis outage → add .on('error') handler. (3) winget local Postgres failed (exit 6) — irrelevant, embedded-pg is the gate.
- Outcome: Advancing to SHIP. Dispatching devops-deployer (clean Phase-0 milestone commit; NO remote/cloud yet → no push/deploy, document the deploy seam) + docs-dx (create missing checklist-0.4 files: CLAUDE.md, README, ARCHITECTURE, CHANGELOG; DX onboarding test).

## 2026-06-23 — SHIP + RETRO — M1 (CLOSED)
- Shipped: milestone commit 4f72e4d tagged `phase-0`; docs commit d6df306 (CLAUDE.md, README, ARCHITECTURE, CHANGELOG, DEPLOY). Health gate green (5/5 RLS exit 0), no secrets tracked, CI workflow .github/workflows/ci.yml CONFIRMED present (docs-dx wrongly reported it missing — verify agent claims vs tree). No remote/push (local-first); deploy seam documented in docs/DEPLOY.md.
- Phase 0 DoD: ALL MET. (a) per-tenant themed render from real DB w/ zero cross-leak; (b) admin edit→storefront via revalidateTag proven; (c) RLS suite green in CI (embedded-pg, no Docker); (d) withTenant the sole tenant path (ESLint-enforced, grep-clean).
- RETRO learnings: (1) RESEARCH caught the app_runtime NOLOGIN blocking defect before any code — de-risking high ROI. (2) No-Docker env → embedded-postgres pivot made the gate run ANYWHERE (better local-first than Docker dependency); adopt as standard. (3) Parallel build with strict file-ownership boundaries (backend infra vs frontend pages) = zero merge conflicts. (4) Harden gauntlet found 1 HIGH + 4 majors a runbook-only check would've shipped — adversarial review paid off. (5) Subagents ran git init + committed without prior ask; now covered by autonomy grant — keep commits local, no push without founder. (6) Always verify agent factual claims (CI "missing") against the actual tree.
- Milestone M1 → DONE (2026-06-23). LOOP → M2 (Phase 1 MVP wedge).

## 2026-06-23 — RESEARCH/PLAN — M2 (Phase 1)
- Decided (autonomous default calls, brief: docs/research/phase1-brief.md): bKash Tokenized Checkout vs PUBLIC SANDBOX (real, testable now); SaaS billing manual record in P1. Steadfast real adapter, contract-tested (NO sandbox → live verify deferred to merchant account). Auth: keep HMAC dev-login as LOCAL provider + build Supabase Auth provider behind same getSession() seam (supabase-local needs Docker=absent / or cloud). Address: bangladesh-location-data npm. SMS: sms.net.bd adapter (live-send deferred to account+masking). Checkout: one withTenant txn (customer upsert→inventory decrement→orders→items→payment) + webhook_event idempotency.
- FOUNDER ACTION flagged (time-sensitive, parallel): start bKash merchant onboarding (~2-4wk), get Steadfast merchant account (no sandbox), decide Docker-for-supabase-local vs Supabase cloud. Surfaced to founder; build proceeds on sandboxes/seam meanwhile (no fake services — real adapters, real bKash sandbox).
- Honest scope note: fully local-testable now = products/variants/images, orders+manual entry, customers, dashboard, storefront, COD checkout end-to-end, bKash via sandbox, address pickers, idempotency, super-admin, provisioning logic, manual billing. Live-deferred (need accounts/Docker): Steadfast live consignment, SMS live send, Supabase cloud auth.
- Dispatched: ceo-architect (Phase-1 blueprint on Phase-0 contracts) + designer (extend DESIGN.md for Phase-1 surfaces: full admin shell/dashboard/orders/products, mobile checkout w/ address pickers + COD/bKash, manual order entry).
- Outcome: pending blueprint → self-approve GATE 1 → build slices.

## 2026-06-23 — GATE1 (self-approved) — M2
- Decided: Phase-1 blueprint (docs/architecture/phase1-blueprint.md) + DESIGN.md Phase-1 surfaces appended. GATE 1 SELF-APPROVED (autonomous). Default calls on the 6 open questions: (1) bKash Tokenized + manual SaaS billing; (2) signup → subscription trialing/plan starter/+14d (starter-level trial for activation); (3) grace 3 days; (4) dev-login local default, Supabase provider built behind getSession seam dormant until AUTH_PROVIDER=supabase+Docker/cloud; (5) root myhybrid.com placeholder; (6) low-stock ≤5.
- 3 new packages: @hybrid/payments (bKash sandbox-real + COD), @hybrid/couriers (Steadfast contract-tested), AES-256-GCM credential crypto in @hybrid/db. Checkout = ONE idempotent withTenant txn (customer upsert→atomic decrement→orders→items→payment, payment.id=idempotency key, webhook_event replay guard). Slices in 4 waves.
- Dispatched: Wave 0 foundation (parallel BE) — S-CRYPTO + S-PAY-PKG + S-COUR-PKG (pure packages bundle) and S-COMMERCE-CORE (lib/commerce + integration tests). Publishes contracts for Waves 1-3.
- Outcome: Wave 0 building. Then Waves 1→3, harden, self-approve GATE 2, ship.
- Commit policy reminder: subagents commit locally only (no push) under autonomy; founder push on request.

## 2026-06-23 — BUILD Wave 0 — M2
- Done: @hybrid/payments (21 tests +2 sandbox-gated), @hybrid/couriers (16), @hybrid/db crypto (8 incl GCM tamper), lib/commerce placeOrder+customer (6 integration incl oversell race=exactly-one-wins, server-side pricing, cross-tenant RLS). All green. Committed.
- Resolved tech debt: Windows EBUSY root-caused = Defender scanning in-repo .pgtmp → PGTMP_DIR env override (point at %TEMP%); CI/Linux default unchanged. Fixed APP_ENCRYPTION_KEY (was 24-byte invalid → valid base64 32-byte dev key).
- Contracts published: sealCredentials/openCredentials; PaymentProvider(BkashProvider/CodProvider)+mapBkashState; CourierAdapter(SteadfastProvider)+mapSteadfastStatus; placeOrder(input):{orderId,orderNumber,paymentId,bkashRequired}; upsertCustomerByPhone(tx,...). shipping_address jsonb shape {recipient,phone,division,district,thana,line}.
- Next: Wave 1 (admin domain catalog/orders/customers/dashboard + auth-provision + shared lib/location), then Wave 2 (checkout/courier-wire/settings/sms), Wave 3 (platform/billing/marketing).

## 2026-06-23 — BUILD Wave 1 — M2
- Done: Admin domain (catalog/orders+manual-entry/customers/dashboard, StatusBadge+Stepper, lib/location shared, lib/storage BlobStore, /api/admin/upload) 30/30 db tests + 16-page build; Auth-provision (getSession supabase branch behind seam dev-default, 05_auth.sql guarded trigger, provisionTenant) 22/22. Committed.
- Contracts: provisionTenant({userId,storeName,slug,plan?}):{tenantId,slug} + createAppUser + SlugTakenError; StatusBadge/StatusStepper in @hybrid/ui; lib/location getCascade; lib/storage getBlobStore.
- Notes/flags: Windows embedded-PG is WIN1252 (can't store Bangla in test DB → verify Bangla render on UTF-8 Docker/Supabase/Linux CI); cod_status derived (cod_amount>0 && unpaid) until shipment exists (Wave 2); middleware supabase token-refresh deferred to middleware owner before live Supabase cutover.
- Next: Wave 2 — S-CHECKOUT+S-SMS (storefront checkout/cart/bkash callback/sms), S-SETTINGS+S-COURIER-WIRE (encrypted creds settings, sendToCourier/courier-sync/COD list).

## 2026-06-23 — BUILD Wave 2 VERIFIED + COMMITTED — M2
- Combined two-agent tree (checkout+sms, settings+courier-wire) verified GREEN: typecheck 5/5, lint 5/5, db suite 40/40 (courier-wire 7, provision 3, rls 5, crypto 8, + checkout/commerce/customer), Next build OK (all routes: /admin/cod, /admin/settings/{courier,payments,store}, /_sites/[tenant]/products/[slug], /api/bkash/callback, /api/internal/courier-sync). Seams stitched: SendToCourierButton in order detail, COD+Settings in AdminNav.
- Stale typecheck errors from pre-compaction (TS2307 @/ leak, TS2345 SealedSecret→JSONValue) were ALREADY fixed by the agents (tsconfig @/* path map + toJsonRecord(... as unknown as Record) cast). Re-verify against tree confirmed both resolved.
- Ops: C: drive full crisis RESOLVED (was 184K free → now 6.6G). Dropped PGTMP_DIR/TMPDIR redirect-to-D: (it broke embedded-pg temp path on Windows); default Windows temp works with space freed.
- Committed locally 4975b16 (no push). LOOP → Wave 3.

## 2026-06-23 — BUILD Wave 3 (dispatched) — M2
- Dispatched 2 parallel agents, strict file-ownership, NEITHER commits (CEO verifies combined + commits):
  - Agent A (backend) S-PLATFORM+S-BILLING: app/(platform)/** super-admin (tenant directory/suspend/impersonate via asPlatformAdmin), lib/billing/status.ts evaluateTenantBilling (trialing→past_due 3d grace→suspended), /api/internal/billing-sweep (CRON_SECRET), lib/platform/**. db test for billing transitions.
  - Agent B (frontend) S-MARKETING: app/(marketing)/** Bengali landing + signup page+action calling provisionTenant (Wave-1 contract) → live subdomain, success→admin.
- Contracts in play: provisionTenant({userId,storeName,slug,plan?}):{tenantId,slug}+SlugTakenError; resolve.ts requires tenant.status='active' (billing enforcement is free at storefront). Golden Rule enforced; no stubs; Bengali+mobile-first.

## 2026-06-23 — HARDEN gauntlet (M2 Phase 1) — findings
- Ran parallel gauntlet on Phase-1 diff (vs phase-0 tag 4f72e4d): ceo-code-reviewer, qa-engineer, security-officer + automated commit-review hook.
- code-review: FIX-FIRST 2 blockers + 4 majors. qa: PASS-WITH-GAPS (51/51 db, payments 21+2skip, couriers 16, web build 33 routes; gap=resolveTenantByHost untested). security: SHIP-AFTER-FIXES 0 crit/0 high/3 med (verified-correct: crypto, masked reads, NO bkash forgery path, billing-sweep auth, impersonation prod-gate, RLS/no-raw-sql clean, no SSRF, upload safe, IDOR safe). commit-hook: 1 HIGH signup account-takeover.
- MUST-FIX before GATE2: (1)BLOCKER bKash amount not verified vs total — thread amount thru Execute/QueryPaymentResult, compare exact, mismatch→failed. (2)BLOCKER courier sync cod_status='collected' fabricated on delivery — use non-terminal state, leave money owed until real remittance. (3)HIGH signup: refuse pre-existing email (createAppUser return created flag), prod-gate dev-cookie branch. (4)MAJOR payment(provider,provider_ref) unique index. (5)MAJOR on suspend move subscription→expired (unblock re-signup vs subscription_one_active). (6)MAJOR refuse cancel on paid order (P1 no auto-refund). (7)MAJOR dashboard low-stock multi-variant subquery crash → proper aggregation. (8)MED XSS javascript: scheme in store facebook url — zod https-only + safeUrl render guard. (9)MED courier-sync constant-time CRON compare; bKash callbackURL derive server-side from verified domain. (10)QA: add resolveTenantByHost liveness test (trial serves, suspended null).
- Lightweight rate-limit on signup+checkout via existing Upstash (else log fast-follow). Dispatching 2 fix agents (clean file ownership), NEITHER commits; CEO re-verifies + commits, then re-runs gauntlet deltas → self-approve GATE2.

## 2026-06-23 — HARDEN fixes + GATE2 (self-approved) — M2
- All 10 must-fix items implemented by 2 fix agents, combined-tree re-verified GREEN: typecheck 5/5, lint 5/5, db 63/63 (10 files: +payment-verify, +resolve, +admin low-stock/cancel, billing expired, courier-wire corrected), web build OK. CEO cleaned 2 cross-agent orphans (stale `origin` in checkout.test, unused beforeAll import).
- Focused adversarial RE-VERIFY of the 2 highest-stakes fixes (bKash amount = money forgery; signup = account takeover): BOTH CONFIRMED-FIXED, no bypass/regression/race. bKash amount threaded from real gateway body, paisa-exact, fails-closed on missing amount, mismatch→failed, replay guard intact, single paid-write path. Signup created-flag (xmax=0) correct, refuses existing email, prod-gates dev-cookie before any cookie set, userId not attacker-controllable/never platform-admin, orphan-cleanup can't delete others.
- GATE 2 SELF-APPROVED (autonomous): QA=PASS(63/63 + liveness gap closed), code-review=SHIP(blockers+majors fixed+reverified), security=SAFE-TO-SHIP(HIGH+mediums fixed+reverified). Harden fixes committed 031f925.
- Documented deferrals (account/infra-gated, flagged at PLAN, NON-blocking): bKash live sandbox round-trip (BKASH_SANDBOX gated), Steadfast live consignment (no sandbox/needs merchant acct), SMS live send (needs account+masking), Supabase cloud auth (dev-login prod-gated, Supabase seam dormant), Bangla-in-DB render verify on UTF-8 (Windows harness WIN1252). bKash callbackURL now server-derived; rate-limit live; refund path = manual P1.
- Outcome: → SHIP. Dispatch devops-deployer (milestone commit/tag phase-1, health gate, document deploy seam, NO push/deploy — no remote, local-first) + docs-dx (CHANGELOG/README/ARCHITECTURE/CLAUDE.md → Phase 1; DX onboarding test). Then RETRO → LOOP M3.

## 2026-06-23 — SHIP + RETRO — M2 (Phase 1 CLOSED)
- Shipped LOCAL milestone: annotated tag `phase-1` on 031f925; docs commits e01e894 (CHANGELOG/README/ARCHITECTURE/CLAUDE.md→Phase1) + fa4dc1a (.env.example/turbo globalEnv: AUTH_PROVIDER,CRON_SECRET,SMS_LIVE,SMS_API_KEY,BLOB_DRIVER) + 30fdff3 (DEPLOY.md §7 Phase-1 env+deferrals) + 35cc7ce (CI APP_ENCRYPTION_KEY valid base64-32 fix + state). NO push/deploy/remote (local-first; founder pushes on request).
- Health gate green: typecheck 5/5, lint 5/5, db 63/63 (10 files), web build 22 routes. Secrets clean (only .env.example tracked; env-only keys, fail-fast). CI runs the @hybrid/db RLS+integration suite as the gate.
- Phase-1 DoD MET: seller self-signup→live trial subdomain→products→COD & bKash(sandbox) order via one idempotent withTenant txn (server-priced, oversell-safe, replay+amount-verified)→Steadfast courier wire+sync→manual billing state machine→encrypted creds→super-admin. RLS sacred throughout (no-raw-sql clean); no stubs in shipping code.
- RETRO learnings: (1) Concurrent-agent shared-file edits leave orphans (stale `origin` field, unused import, in-flight requestHeaders race) — CEO MUST re-verify the COMBINED tree, never trust per-agent green. Cheap orphan-fixes by CEO kept momentum. (2) Strict file-ownership briefs (A=platform/billing, B=marketing; A=core, B=auth/sec) prevented destructive collisions — the 2 collisions that occurred were on the boundary file (checkout.test) and resolved trivially. (3) Harden gauntlet caught 2 money/auth BLOCKERS (unverified bKash amount; fabricated COD-collected) + 1 HIGH (signup takeover) a runbook check would've shipped — adversarial review is the highest-ROI gate. (4) Focused re-verify of just the 2 highest-stakes fixes (money+auth) before GATE2 beat re-running the whole gauntlet. (5) Latent CI bug (invalid APP_ENCRYPTION_KEY) found by DX agent only because docs-dx actually read CI vs code — verify config against runtime, not assumptions. (6) Windows embedded-pg flake (orphan PG + stale .pgtmp + AV race + RAM pressure) is the dominant local-ops friction; sweep before each gate. PGTMP_DIR redirect to D: BROKE embedded-pg temp paths — don't; just free C:.
- LOOP → M3 (Phase 2: custom domains + theme catalog + visual customizer + COD reconciliation). RESEARCH next.

## 2026-06-23 — CHARTER LOOP — M3+M4+M5 (founder authorized full sweep)
- Decided: Founder authorized full Phase 2+3+4 sweep ("Phase 2 + 3 + 4 (all)"). Two architectural shifts mandatory this run.
- SHIFT 1 — DROP Supabase entirely. Postgres provider-agnostic (Neon/Railway/RDS/self-hosted/any PG16+). Auth: own implementation — extend HMAC dev-login seam → durable session (Argon2id-hashed passwords, secure HttpOnly cookies, CSRF) + email+phone OTP via tenant SMS/email provider. Storage: S3-compatible BlobStore (R2/B2/S3/MinIO) replacing planned SupabaseBlobStore. Remove SUPABASE_* env vars. Gate or drop 05_auth.sql trigger. RLS + withTenant() unchanged (provider-agnostic — works on any vanilla PG with the bookend roles).
- SHIFT 2 — PER-TENANT CRED MODEL. No founder-onboarded gateway/courier/SMS creds. Each tenant configures their own bKash/Nagad/SSLCommerz/Steadfast/Pathao/RedX/Paperfly/SMS creds via admin Settings UI, sealed with APP_ENCRYPTION_KEY (extends Phase-1 pattern already proven for bKash+Steadfast). Founder retains no provider liability.
- Why: Founder's strategic call — tenants bring their own infrastructure; product remains pure SaaS layer. Drops vendor lock, expands deployable footprint (any PG host), removes single-tenant gateway scaling bottleneck.
- Implications: provisionTenant() can't assume Supabase Auth event-driven user creation — own signup flow creates app_user + sends OTP. Phase-2 settings UI must support every payment + courier + SMS provider configurable per tenant. Tests must remain provider-agnostic (embedded-pg already is; no Supabase test dep).
- Gating model this sweep: GATE 1 + GATE 2 SELF-APPROVED per autonomy grant (M1→M5), but founder paged via PushNotification at each gate. Standing rules unchanged: GATE 2 self-approve only if QA=PASS + code-review=SHIP + security=SAFE-TO-SHIP (or all blockers fixed).
- Dispatched: researcher (Phase 2 brief — Vercel Domains API + custom domain verify + auto-SSL contracts; section-based theme editor patterns; visual customizer state model; multi-courier adapter unification; COD reconciliation algorithm; GA4 server-side + FB CAPI conversions; WhatsApp Cloud API; Argon2id Node bindings + OTP patterns; S3-compatible BlobStore minimal API; Postgres provider-agnostic verification — known gotchas across Neon/Railway/RDS).
- Outcome: pending researcher return. Then ceo-architect + designer (Phase 2 blueprint + design surfaces for customizer/funnels) parallel → self-approve GATE 1.

## 2026-06-24 — RESEARCH (re-dispatch) — M3
- Decided: Founder re-invoked /ceo "Complete rest of the phases" — confirms full M3+M4+M5 sweep under standing autonomy. Prior RESEARCH dispatch never landed (docs/research/phase2-brief.md is a 3-line stub). Re-running RESEARCH for M3 with TWO parallel researcher tracks to de-risk before PLAN.
- Why: M3 = 8 features + 2 mandatory architectural shifts (drop Supabase / own-auth, per-tenant creds). The shifts are foundational and block everything else — they need their own focused brief separate from the feature research.
- Dispatched (parallel): researcher-A (architectural shifts: drop-Supabase PG-provider-agnostic verify, own-auth Argon2id+session+CSRF+email/phone OTP, S3-compatible BlobStore minimal driver, per-tenant cred model extension); researcher-B (Phase-2 features: Vercel Domains API custom-domain+verify+auto-SSL, theme catalog + constrained visual customizer state/JSON model, discounts at checkout, multi-courier adapter unification Pathao/RedX/Paperfly, COD reconciliation algorithm + schema, GA4 server-side + FB CAPI, WhatsApp Cloud API).
- Outcome: pending both briefs → synthesize → ceo-architect + designer PLAN → self-approve GATE 1 + notify founder.

## 2026-06-24 — PLAN — M3
- Decided: Both research briefs landed — phase2-brief-shifts.md (arch shifts) + phase2-brief-features.md (Phase-2 features). Key findings adopted: withTenant() needs ZERO changes on vanilla PG16 (Neon direct endpoint, pgBouncer txn-mode safe, prepare:false already set, two-role bookend works via CREATEROLE); @node-rs/argon2 (not `argon2` — fails on Vercel) + opaque session token hashed in user_session table + SameSite=Lax (multi-subdomain) + OTP table with NULLABLE user_id (signup before user row); drop 05_auth.sql from migrations, add 06_own_auth.sql; S3BlobStore via @aws-sdk/client-s3 endpoint-override (R2/B2/MinIO/S3); per-tenant creds extend sealCredentials pattern to all providers. Existing tenant_domain/theme/tenant_theme_settings/discount/cod_remittance tables already largely defined. All 7 features local-testable at data/logic layer; live-deferred = Vercel API, courier live accounts (Pathao stage env available), GA4/CAPI reporting, WhatsApp template approval.
- Blocking/founder items surfaced: (a) RedX + Paperfly have NO public API docs → defer those adapters, Pathao ships first (has stage env); (b) Nagad/SSLCommerz require tenant to whitelist callback/IPN URLs in their portal → Settings UI must show exact URLs + copy + Test Connection or callbacks silently fail; (c) Vercel Pro plan + project needed for live custom domains (build+test local behind flag); (d) WhatsApp Bengali Utility template must be submitted to Meta by founder before release; (e) per-courier remittance CSV column names need a real Steadfast report before writing parsers.
- Why: De-risked both the foundational shifts and every feature before committing a design. Architect now has concrete schema/algorithm recommendations to build a buildable wave-sequenced blueprint.
- Dispatched (parallel): ceo-architect (M3 blueprint — sequence arch shifts as Wave 0 foundation, then feature waves; resolve open architect-decisions from both briefs) + designer (Phase-2 design surfaces: visual customizer UI, theme catalog/picker, COD & Settlements view, per-provider Settings UI with callback-URL display + Test Connection, custom-domain connect flow, discounts admin).
- Outcome: pending blueprint + design → synthesize → self-approve GATE 1 (autonomy) + PushNotification founder → BUILD waves.

## 2026-06-24 — GATE1 (self-approved) + BUILD Wave 0 — M3
- Decided: PLAN complete. Blueprint docs/architecture/phase2-blueprint.md (6 sections, P1 rigor, verified vs shipped tree) + DESIGN.md Phase-2 surfaces §Q0–Q8 (zero new tokens, 9 new shared UI components incl ProviderCard/CopyField/OtpInput/SectionToggleRow). GATE 1 SELF-APPROVED (autonomy M1→M5); founder is interactive this session — surfaced GATE-1 summary + 5 founder-action items in-chat instead of PushNotification.
- Wave plan: W0 arch-shifts foundation (BLOCKING) → (W1 features ∥ W2 courier+COD) → W3 analytics+whatsapp. New migrations: 06_own_auth.sql (user_session, otp_code NULLABLE user_id, app_user.password_hash; self-or-admin RLS + explicit grant to app_runtime — default privileges don't cover later-migration objects, footgun caught by architect); 07_phase2.sql (cod_remittance +status/processed_at/unmatched_count); DROP 05_auth.sql from migrate list. All other Phase-2 features reuse already-shipped tables.
- Founder-action items surfaced (parallel, non-blocking): (1) WhatsApp Bengali template → Meta (24-48h, critical path); (2) real Steadfast remittance CSV for COD parser columns; (3) confirm Vercel Pro for live domains; (4) RedX+Paperfly API access (skeleton-only till then); (5) pick 3 starter themes.
- Dispatched Wave 0 — 3 parallel backend agents, STRICT file ownership, NONE commit (CEO verifies combined tree + commits per P1 learning):
  - A1 OWN-AUTH vertical: packages/db/sql/06_own_auth.sql + migrate.ts(add 06/drop 05) + apps/web/lib/auth/** (Argon2id @node-rs/argon2, opaque session table, OTP) + getSession provider swap + signup/login/otp route handlers + CSRF Origin check + provision.ts own-user-creation.
  - A2 STORAGE+CREDS: apps/web/lib/storage/** (S3BlobStore @aws-sdk/client-s3 endpoint-override) + packages/payments/src/{nagad,sslcommerz} + packages/couriers/src/{pathao + redx/paperfly skeletons} + per-tenant cred plumbing.
  - A3 ENV+CONFIG: .env.example (remove SUPABASE_*, add AUTH_/SESSION_SECRET/S3_/provider vars), turbo.json globalEnv, next.config.mjs (serverExternalPackages @node-rs/argon2), docs env. Small slice.
- Outcome: Wave 0 building. CEO re-verifies combined tree (typecheck/lint/db-suite/build) + commits, then W1∥W2 → W3 → HARDEN gauntlet → GATE 2.

## 2026-06-24 — BUILD Wave 0 VERIFIED + COMMITTED — M3
- Wave 0 returned green. CEO combined-tree verify (the P1 discipline): pnpm install picked up @node-rs/argon2 (+win32 native) and @aws-sdk/client-s3 into workspace node_modules (NOT root-hoisted — pnpm); the password.ts "arity" errors were missing-module artifacts, gone after install. Final: typecheck 5/5, lint 5/5, db 81/81 (13 files: +password/session/otp), payments 37 (+Nagad8/SSLCommerz8), couriers 41 (+Pathao15/skeletons10), web build OK (new /api/auth/* + /signup routes). Committed locally (no push) — own-auth replaces Supabase end to end, S3 driver, 3 new providers + 2 skeletons.
- Flags: (a) stray scratchpad_partC.b64.txt got committed — purge at SHIP. (b) types.ts not regenerated (db:gen needs live DIRECT_URL; tests green w/o it — regen after a DB is up). (c) auth login/signup UI PAGES deferred (dev-login works locally; real pages land with W1 settings/auth surfaces). (d) SESSION_SECRET declared but unused (opaque tokens validated by DB lookup, not signature) — keep for now, prune at harden.
- Dispatched core M3 feature wave (W1∥W2), 4 parallel agents, STRICT file + packages/ui-component ownership, NONE commit (CEO verifies+commits):
  - W-THEME (BE+FE): theme catalog (3 themes) + constrained visual customizer (draft/publish JSON, ?preview admin-gated). Owns lib/storefront/data.ts, admin/themes + customizer pages, ui/SectionToggleRow. Hard no-drag.
  - W-DOMAINS+SETTINGS (BE+FE): custom domains (Vercel flag-gated, tenant_domain state machine, middleware/resolve extension) + per-tenant provider Settings UI for ALL providers w/ callback-URL CopyField + Test Connection. Owns middleware.ts/resolve.ts domain bits, admin/settings/**, admin/settings/domains, ui/{ProviderCard,CopyField,ToggleSwitch,TestConnectionButton}.
  - W-DISCOUNTS (BE+FE): coupon apply inside placeOrder txn (SELECT FOR UPDATE, server-priced) + discounts admin + checkout field. Owns placeOrder.ts (sole), admin/discounts, checkout discount UI. Reuses existing discount table (no migration).
  - W-COD+PATHAO (BE+FE): 07_phase2.sql (cod_remittance +cols) + migrate.ts(add 07, sole) + lib/cod CSV ingest/match/discrepancy + COD & Settlements view + Pathao wired into sendToCourier/sync + courier settings. Owns lib/cod/**, lib/couriers app-wiring, admin/cod settlements, ui/{DeltaAmount,DiscrepancyStat,EmptyState,Skeleton}.
- Deferred to W3: analytics (2.7 GA4/CAPI), WhatsApp (2.8). Auth UI pages folded where surfaced.
- Outcome: feature wave building → CEO verify combined tree + commit → W3 → HARDEN gauntlet → GATE 2.


## 2026-06-24 — BUILD Wave 1+2 VERIFIED + COMMITTED — M3
- Resumed mid-BUILD: prior session dispatched W1∥W2 (themes/customizer, domains+settings, discounts, COD+Pathao) but died before the CEO combined-tree verify+commit. All feature files were present uncommitted. Ran the P1 combined-tree discipline.
- Fixed cross-agent orphans + real bugs found at integration: (1) THEME shared-mutable catalog ref — themeDefaults() returned THEME_CATALOG[0].defaults by reference; a test's in-place color mutation poisoned the process-wide palette → structuredClone (real prod hazard, caught by frontend-engineer). (2) storeName relaxed to optional default "" (sourced from tenant record, not preset; storefront already falls back). (3) sync.ts public sig widened CourierBindings|CourierAdapter. (4) noUncheckedIndexedAccess assertions in SectionControls + theme index. (5) fonts.ts Fraunces weight:"variable" (axes illegal with explicit weight list — was breaking the build). (6) test harness embedded-pg initdb --encoding=UTF8 --locale=C → Bangla round-trips on Windows too, CLOSES tech-debt known-issue #4 (125/125 now green locally incl theme activation Bangla store-name).
- Gate GREEN: typecheck 5/5, lint 5/5, db 125/125 (16 files incl +theme/+domains/+cod-recon), web build OK (OOM crash on default heap → NODE_OPTIONS=--max-old-space-size=6144; build worker exit 3221225477/134 was OOM not code). Committed bca6f6b (local, no push).
- Founder note (2026-06-24): VPS + Docker available ("use it for whatever work is needed"). Relevant for the real Linux production build + live-integration verification (bKash/Steadfast/Pathao live, custom-domain SSL); local-first build now green via heap bump so not blocking. Will use VPS at SHIP / live-verify.
- Deferred still: W3 = 2.7 analytics (GA4/Pixel/CAPI) + 2.8 WhatsApp. Dispatching W3 next, then HARDEN gauntlet → GATE 2.

## 2026-06-25 — BUILD Wave 3 COMMITTED + HARDEN parked — M3
- Wave 3 (analytics 2.7 GA4+Pixel/CAPI + WhatsApp 2.8) was already scaffolded; verified + integration-fixed (test-only type casts) + committed 062f14a. M3 BUILD COMPLETE: all 8 Phase-2 features (2.1–2.8) + Wave-0 arch shifts landed across 1ff9a69/bca6f6b/062f14a.
- Gate GREEN: typecheck 10/10, lint 10/10, db 138/138 (+analytics 5, +whatsapp 8), web build OK. Build needs --max-old-space-size=6144 (default heap OOMs/SIGABRTs on this box, exit 3221225477/134 = env not code); typecheck/lint need --concurrency=1 to avoid the same OOM. Closed tech-debt #4 (UTF8 embedded-pg → Bangla round-trips on Windows).
- HARDEN gauntlet dispatched (ceo-code-reviewer + qa-engineer + security-officer over git diff 031f925..HEAD) but ALL THREE hit the API session limit (resets 9:40pm Asia/Dhaka) and returned 0 findings — gauntlet did NOT run. PARKED at HARDEN. Per standing rule, no GATE-2 self-approve without QA=PASS + review=SHIP + security=SAFE-TO-SHIP. Re-run the gauntlet after the session-limit reset, fix any blockers, then GATE 2 → SHIP → RETRO → LOOP M3.5.
- Founder (2026-06-25): confirmed VPS + Docker available — earmarked for the real Linux production build + live-integration verification at SHIP (local Windows build green via heap bump, so not blocking now).

## 2026-06-25 — INFRA — migrated entire backend to self-hosted Supabase on the VPS
- Decided: Per founder, self-host Supabase on the VPS and migrate the WHOLE backend to it — **including auth** (founder explicitly chose "Full Supabase incl Auth" over keeping the already-built own-auth, with full knowledge of the rework). DB + Auth + Storage now on self-hosted Supabase; app live at hybrid.ecomex.cloud. **This reverses the earlier "Phase 2 drops Supabase" decision** — that plan is now historical.
- Why: Founder wants the whole backend on self-hosted Supabase (Studio-managed users, one ecosystem) on owned infra rather than Vercel/Supabase-Cloud.
- Done: (1) Discovered a full Supabase stack already on the box (Coolify, ref pe9o2li2n3bns3wnofob49uw) but OOM-killed on 8 GB — trimmed it (dropped analytics/vector/realtime/edge-functions/supavisor) and run it lean from docker-compose.trimmed.yml WITHOUT Coolify. (2) Wiped a different project's data from the shared Supabase (founder-approved; safety-dumped first) and loaded Hybrid schema/seed into postgres.public; RLS isolation re-verified. (3) DB cutover (DATABASE_URL/DIRECT_URL → supabase-db; migrated production tenant_domain rows). (4) Auth: GoTrue as credential authority + app opaque session (lib/auth/supabaseAuth.ts; supabase branches; /login page; @supabase/supabase-js); 3 users provisioned. (5) Storage: BLOB_DRIVER=s3 → MinIO, bucket hybrid-media, public CDN cdn.hybrid.ecomex.cloud (GetObject-only). (6) Fixed force-dynamic prerender bug on auth shells. (7) Repo docs updated (CLAUDE.md production callout, docs/INFRA_SUPABASE.md runbook, .env.example, compose/Caddyfile sync).
- Outcome: All surfaces 200, login→dashboard verified, tenant isolation intact, upload→CDN verified, ~4.3 GB RAM free. Legacy hybrid-postgres kept as rollback net (retire after soak). Future agents: treat self-hosted Supabase as the backend for DB+auth+storage; do not reintroduce Vercel/Upstash/own-auth-replaces-Supabase assumptions.

## 2026-06-25 — HARDEN gauntlet + fixes DEPLOYED to prod — M3
- Founder migrated backend to self-hosted Supabase out-of-session (DB=supabase-db, AUTH=GoTrue+opaque session, STORAGE=MinIO; live hybrid.ecomex.cloud). Verified live: 35 tables, both RLS roles, RLS isolation PROVEN as app_runtime_login (cross-tenant SELECT=0, INSERT RLS-blocked), 3 GoTrue↔app_user, routes 200/307/200.
- Ran the parked HARDEN gauntlet (code-review + QA + security) over 031f925..HEAD incl migration. Verdicts: code FIX-FIRST (2 blockers), security SHIP-AFTER-FIXES (2 crit/4 high), QA PASS-WITH-GAPS. CRITICAL-2 (supabase ports public) verified NOT real (Caddy-only 80/443).
- backend-engineer fixed all: bKash analytics payload MERGE (3 sites) [BLOCKER], COD recon duplicate-consignment dedup [MAJOR], rate-limit XFF right-most + auth fail-closed [HIGH], upload requireSameOrigin [HIGH], Caddy security headers + CDN GET/HEAD+path lockdown [HIGH], .env.example key placeholder + accepted_at parity + stale-comment fixes [MED]. Committed ad8a669. Gate: typecheck/lint 10/10, db 139/139 (Windows rls flake cleared on clean boot), web build OK (returns WIP set aside).
- DEPLOYED ad8a669 to prod: git-archived ONLY the 15 committed harden files → /opt/hybrid (preserved live .env.deploy + founder's untracked cache-handler/next.config wiring), deploy.sh rebuild + recreate. Canary GREEN: headers live, routes ok, CDN list/PUT/other=403, web still on supabase-db. Single-file-bind-mount inode gotcha noted (Caddyfile needs container recreate, not just file replace).
- OPEN (non-blocking): (1) live AUTH_PROVIDER=supabase GoTrue path has NO automated test — add fast-follow. (2) founder's returns feature unfinished (missing CreateReturnForm + mapReturnRow type err) — preserved untracked, breaks build if included. (3) cache-handler.cjs + next.config/eslint wiring uncommitted on local (live on VPS) — founder to commit. (4) CDN public via Cloudflare returns 000 (Cloudflare↔origin SSL mode for cdn subdomain) — pre-existing, founder Cloudflare fix. (5) off-site backups (backlogged). (6) legacy hybrid-postgres still up as rollback net — retire after soak.
- NEXT: M3 effectively hardened+shipped. LOOP → M3.5 (BD moat: F-commerce automation, COD fraud score, escrow, DBID, SLA — docs/research/roadmap-gap-plan.md) per backlog, then M4/M5.

## 2026-06-25 — M3 prod-gap closure — M3
- Founder chose "close M3 prod gaps first." Closed: (1) live Supabase/GoTrue auth test (90bbd62, 7 tests + injectable seam); (2) returns WIP parked on branch wip/returns (master clean, builds); off-site R2 backups already done by founder.
- CRITICAL surfaced: Cloudflare Universal SSL only covers *.ecomex.cloud (1-level) → ALL *.hybrid.ecomex.cloud 2-level hosts (tenant storefronts store-a/store-b, admin, app, cdn) fail TLS at CF edge = 000 publicly; only apex marketing works. Caddy origin certs valid. PRODUCT-DOWN for tenant subdomains. Needs founder CF action (ACM wildcard / Total TLS for *.hybrid.ecomex.cloud, recommended; or grey-cloud + Caddy on-demand-TLS). Cannot fix without CF token/dashboard (none on box).
- Tech-debt surfaced: test suite shares one embedded-pg DB across 22 files → cross-file unique-key flake (passes per-file). Needs per-file data isolation.

## 2026-06-25 — CRITICAL RESOLVED: tenant-subdomain TLS — M3
- Founder provided a CF DNS-edit token. Root cause confirmed: *.hybrid.ecomex.cloud + apex were Cloudflare-PROXIED, but Universal SSL only issues for 1-level (*.ecomex.cloud) → 2-level edge cert missing → all tenant/admin/app/cdn subdomains TLS-failed (000) publicly.
- FIX APPLIED: PATCH'd the wildcard DNS record *.hybrid.ecomex.cloud to proxied=false (grey-cloud) via CF API. Traffic now hits Caddy directly, which holds valid LE certs. Apex left proxied (works + keeps CF cache). VERIFIED: store-a/store-b 200, admin/app 307 (auth), cdn 403 (lockdown), admin/login 200. Tenant storefronts reachable. RLS isolation already proven at DB layer.
- FOLLOW-UPS (non-blocking): (1) on-demand TLS — Caddyfile lists explicit hosts, so a NEW tenant subdomain (store-c+) has no cert and would 000; add Caddy on_demand_tls + an ask endpoint (resolveTenantByHost gate) before onboarding new sellers. (2) grey-cloud loses CF edge cache for storefronts (the <50ms win) — ACM/Total TLS wildcard for *.hybrid.ecomex.cloud restores proxy+cache (needs SSL:edit token, ~$10/mo). (3) MinIO hybrid-media bucket policy=custom, empty — verify GetObject-only serves images when real ones uploaded. (4) ROTATE the CF DNS token + R2 keys (pasted in chat).
